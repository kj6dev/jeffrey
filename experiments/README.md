# Experiments

These files preserve design provenance.

They are **not live requirements**.

- `astra-review-prompt.md` — the canonical pre-review brief sent to a frontier model
- `astra-review-response.md` — adversarial reduction/review that led to the frozen v0
- `spec-kit-spdd-comparison.md` — bounded methodology comparison

The canonical current design lives under `docs/` and `runtime/`.

If an experiment conflicts with the current contract, the current contract wins unless a new explicit decision updates it.
