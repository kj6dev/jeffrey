# Repository package import

The package comes from the “AI Career Direction” chat, conversation `6a9ae9ff-8710-83e8-9874-4ed6e09af061`, following the “Go” turn `a100dfc3-8c75-4a12-9b65-1c34072375cf`. Imported on 2026-09-08 from `~/Downloads/career-os-repo.zip`.

The [original archive](../imports/2026-09-08-career-os/career-os-repo.zip) and [hash manifest](../imports/2026-09-08-career-os/provenance.json) preserve all 22 source files. The package's AGENTS.md is retained as [source evidence](../imports/2026-09-08-career-os/AGENTS.original.md); workspace instructions are merged into the root AGENTS.md. README.md adds a navigation note. All other imported files retain their original contents. The package's MANIFEST.md describes that package, not the complete merged workspace.

## Authority

The broader rationale and deferred ideas live in `docs/`. The immutable `career-os-handoff.md` remains the frozen source contract. `handoff/` and the original trials remain intact as specification evidence. Imported templates are blank design artifacts, not operational state. This import does not execute v0 or authorize deferred capabilities.

## Differences requiring reconciliation before validation

a. `docs/v0-contract.md` matches the earlier implementation contract except its title, authority paragraph, and scenario link. Its claim that `evals/scenarios.md` is unchanged is inaccurate: the packaged file summarizes the cases, while `experiments/shared/scenarios.md` retains concrete synthetic inputs, dates, separate turns, and P1–P6 probes. Use the original pack for those exact inputs and probes.

b. `runtime/behavior-instructions.md` is a rewrite of `handoff/behavior-instructions.md`. It omits some explicit safeguards, including stale-proposal reconciliation, the distinction between approved and confirmed-applied edits, the maintenance agreement, and detailed work-data boundaries. Its conditional communication wording must not be read as expanding the frozen v0 scope. Reconcile against the source contract before treating it as a standalone runtime prompt.

c. `docs/future-capabilities.md` preserves a broader candidate set of sensors, reasoning specializations, derived views, market calibration, and personal pattern discovery. These remain deferred ideas. The ZIP is a snapshot of the chat at packaging time, not a capture of subsequent discussion such as Habitat Discovery.

## Next action

Reconcile the packaged runtime prompt and evaluation references with the frozen safeguards, then run synthetic Decide/Prepare/Close validation on the actual work-approved model. Preserve the original import as evidence while evolving the working documents from explicit decisions and observed failures.
